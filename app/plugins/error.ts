export default defineNuxtPlugin(() => {
  // nuxtApp.vueApp.config.errorHandler = (error, instance, info) => {
  //   console.log(error)
  //   console.log(instance)
  //   console.log(info)

  //   showError({
  //     statusCode: 500,
  //     statusMessage: info || 'An error occurred',
  //   })
  // }

  // // Also possible
  // nuxtApp.hook('vue:error', (error, instance, info) => {
  //   console.log(error)
  //   console.log(instance)
  //   console.log(info)

  //   showError({
  //     statusCode: 500,
  //     statusMessage: info || 'An error occurred',
  //   })
  // })
})
