// Todo layer — reference vertical slice (page-scoped CRUD via provide/inject).
// Auto-discovered by Nuxt because it lives under <root>/layers/.
export default defineNuxtConfig({
  $meta: {
    name: 'todo',
  },
})
