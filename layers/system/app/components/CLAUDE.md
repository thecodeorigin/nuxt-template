# layers/system/app/components

> System/admin components, namespaced by feature folder. Not auto-imported — import
> explicitly via `#layers/system/app/components/...`.

| Path | Default-imported as |
|------|---------------------|
| `System/SystemEmailDispatch.vue` | `SystemEmailDispatch` (recipient filter + compose + preview + send) |

Each filename starts with its parent folder's name. Add a new namespace folder for a
new admin tool group; don't drop flat files at `components/`.
